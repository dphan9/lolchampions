import { Block } from './block';

export default class Recommended {
    blocks: Block[];
    champion: string;
    map: string;
    mode: string;
    priority: boolean;
    title: string;
    type: string;
}